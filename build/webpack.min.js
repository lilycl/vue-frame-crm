let conf = require('./webpack.lib')
let merge = require('webpack-merge')
let { resolve } = require('./helper')
let dist = resolve('lib')

module.exports = merge(conf, {
    externals: {
        vue: {
            root: 'Vue',
            commonjs: 'vue',
            commonjs2: 'vue',
            amd: 'vue'
        }
    },
    output: {
        path: dist,
        publicPath: '/lib/',
        libraryTarget: "umd",
        filename: "frame.js",
        umdNamedDefine: true
    },
})